# LOG

Each entry should be 1-3 lines: date, change, current status.

- 2026-06-26: Read `2022_VLSI_e-G2C.pdf` and `guide_ref/` documents. Scope set to architecture-level Verilog simulation first, with no real weights/dataset available.
- 2026-06-26: User confirmed external research is allowed, e-G2C-specific docs should live under `e-G2C/`, and generic reimplementation docs should live at repo root.
- 2026-06-26: Verified installed tools: `iverilog`, `vvp`, `pdftotext`, `pdfimages`; `gtkwave` is installed but needs a graphical DISPLAY to open waveforms.
- 2026-06-26: Added initial documentation set: root reimplementation guide, e-G2C paper notes, project guide, implementation plan, TODO, and BUGFIX.
- 2026-06-26: Independent plan-review subagent found the first milestone too large and several contracts unclear. Revised the plan with explicit path conventions, generated artifact formats, smaller milestones, Phase 3 subphases, and an assumption register.
- 2026-06-26: Independent reviewer accepted the revised plan with no blocking findings. Addressed remaining small notes by adding `target.json`/`adapt_target.json` to the artifact contract, clarifying `expected.hex`, and aligning TODO with the Phase 4 dense pipeline.
- 2026-06-26: Extracted paper text to `e-G2C/extracted/paper_text.txt` and rendered local page images under `e-G2C/extracted/pages/`. Added extraction manifest and local figure lookup notes; page PNGs remain ignored by Git.
- 2026-06-26: Added the minimal RTL/simulation skeleton: paper-sized behavioral memories, pass-through activation buffers, MAC lane/array modules, controller/top shells, `tb_smoke`, and `sim/run_sim.sh smoke`. Smoke simulation passes.
- 2026-06-26: Added `tb_mac` and the `./sim/run_sim.sh mac` target for signed MAC lane accumulation, clear behavior, and 32-lane array arithmetic. Smoke and MAC simulations pass.
- 2026-06-26: Added deterministic Python golden generation plus `eg2c_dense_conv2d`, `tb_conv`, and `./sim/run_sim.sh conv` for the first dense normal-convolution baseline. Conv target uses NHWC activations, `kh,kw,cin,cout` weights, explicit zero padding, signed int8 data/weights, int32 accumulation, and saturated int8 output.
- 2026-06-26: Added depth-wise convolution golden generation plus `eg2c_dw_conv2d`, `tb_dw`, and `./sim/run_sim.sh dw`. DW target matches Python golden with one kernel element per simulated cycle.
- 2026-06-26: Added point-wise convolution golden generation plus `eg2c_pw_conv2d`, `tb_pw`, and `./sim/run_sim.sh pw`. PW target uses `cin,cout` 1x1 weights and matches Python golden.
- 2026-06-26: Added average-pooling golden generation plus `eg2c_avg_pool2d`, `tb_pool`, and `./sim/run_sim.sh pool`. Pool target uses 2x2 stride-2 signed average with truncation toward zero.
- 2026-06-26: Added `eg2c_dense_pipeline`, `tb_pipeline_dense`, and `./sim/run_sim.sh pipeline_dense` for an instruction-driven CONV -> POOL -> DONE dense toy pipeline. The generated `instr.hex` and `target.json` document the simulation opcode stream.
- 2026-06-26: Fixed a function-hidden vector sensitivity issue exposed by `pipeline_dense`; documented it as BUGFIX #001. Full current regression through `pipeline_dense` passes.
- 2026-06-26: Added `eg2c_detector_branch`, `tb_branch`, and `./sim/run_sim.sh branch` for signed detector-score thresholding between coarse and precise output paths. Boundary rule is `score >= threshold` selects precise.
- 2026-06-26: Verified current full regression after branch integration: `smoke`, `mac`, `conv`, `dw`, `pw`, `pool`, `pipeline_dense`, and `branch` all pass. No new bug record needed for this task.
- 2026-06-26: Added `eg2c_sparse_selector`, `eg2c_sparse_vector_mac`, `tb_sparse`, and `./sim/run_sim.sh sparse`. The sparse target uses project-local vector-major indices/weights, skips invalid all-zero vectors, matches dense dot-product output, and reports active sparse cycles versus dense-equivalent cycles.
- 2026-06-26: Verified current full regression after sparse integration: `smoke`, `mac`, `conv`, `dw`, `pw`, `pool`, `pipeline_dense`, `branch`, and `sparse` all pass. No new bug record needed for this task.
- 2026-06-26: Added `eg2c_dw_reuse_conv2d`, `tb_dw_reuse`, and `./sim/run_sim.sh dw_reuse`. This is an architecture-level output-equivalent DW reuse model with simple/CIR/D-RIR utilization counters, not an ASIC cycle-accurate schedule.
- 2026-06-26: Verified current full regression after DW reuse integration: `smoke`, `mac`, `conv`, `dw`, `pw`, `pool`, `pipeline_dense`, `branch`, `sparse`, and `dw_reuse` all pass. No new bug record needed for this task.
- 2026-06-26: Independent review split across documentation, RTL, test/golden, and simulation flow found issues in path handling, branch signed coverage, sparse stats checking, top/controller scope, and documentation boundaries.
- 2026-06-26: Fixed review findings: made `run_sim.sh` root-stable and strict on argument count; added `$readmemh` X/Z checks; expanded signed branch cases and branch wave support; added sparse total-cycle checking; guarded SRAM address ranges; added pool bounds checks; documented current sparse/DW-reuse scope and top/controller limits.
- 2026-06-26: Verified fixes before re-review: all ten targets pass from repo root; `/tmp` invocation of absolute `sim/run_sim.sh conv` passes without readmemh errors; `branch wave` writes `sim/build/branch/wave.vcd`; extra run_sim arguments now exit with status 2; `iverilog -g2012 -Wall -tnull -I rtl rtl/*.v` passes.
- 2026-06-26: Second independent review split across documentation, RTL, test/golden, and simulation flow found remaining document gaps, controller illegal-opcode/unterminated-program risks, weak pipeline instruction coverage, and missing generated-artifact manifesting.
- 2026-06-26: Fixed second-review findings: added controller `error_o` and instruction bounds, top parameter pass-through, full-accumulator-width assertions, sparse selector index assertion, 4-case `pipeline_dense`, stricter `.bin` checks, sparse stats consistency checks, generated `manifest.json`, and manifest validation.
- 2026-06-26: Verified second-review fixes: all ten targets pass; generated targets print manifest OK; `pipeline_dense` covers 4 programs; `/tmp` absolute invocation of `run_sim.sh conv` passes; `branch wave` writes a non-empty VCD; bad extra args exit 2; `iverilog -g2012 -Wall -tnull -I rtl rtl/*.v` and `git diff --check` pass.
- 2026-06-26: Integrated vector-wise sparse weight skipping into normal-conv and point-wise-conv schedulers. Added `conv_sparse` and `pw_sparse` targets with generated vector-valid masks, dense-equivalent golden outputs, and active/skip/total cycle checks.
- 2026-06-26: Verified new sparse schedule targets: `conv_sparse` passes with 624 sparse cycles vs 864 dense-equivalent cycles; `pw_sparse` passes with 288 sparse cycles vs 400 dense-equivalent cycles after switching to `IN_C=5` for tail-vector coverage. Existing `conv`, `pw`, and `pipeline_dense` targets still pass after the new RTL ports.
- 2026-06-26: First independent review after sparse schedule integration found no blocking issues. Fixed document consistency notes, clarified `wave|--wave`, guarded sparse vector-count elaboration, added dense-mode counter checks, added dense-baseline checks in sparse testbenches, and reran all 12 targets successfully: `smoke`, `mac`, `conv`, `dw`, `pw`, `pool`, `pipeline_dense`, `branch`, `sparse`, `conv_sparse`, `pw_sparse`, and `dw_reuse`.
- 2026-06-26: Second independent review found code, test, and flow checks converged. Fixed one remaining low-priority document wording issue by separating completed sparse scheduling from future 4-bit packed weight-format work.
- 2026-06-26: Replaced the DW reuse analytical counter wrapper with architecture-level simple/CIR/D-RIR lane-assignment schedules. The `dw_reuse` target now checks all three schedule outputs against the same Python golden tensor and validates cycle, active-lane, and idle-lane counters.
- 2026-06-26: Verified DW lane scheduling and full regression. `dw_reuse` passes with simple=576 cycles, CIR=192 cycles, D-RIR=288 cycles, active lane slots=400, and idle lane slots=176 for each mode. Full regression passes for all 12 targets: `smoke`, `mac`, `conv`, `dw`, `pw`, `pool`, `pipeline_dense`, `branch`, `sparse`, `conv_sparse`, `pw_sparse`, and `dw_reuse`. `iverilog -g2012 -Wall -tnull -I rtl rtl/*.v` and `git diff --check` also pass. No confirmed implementation bug was found; no BUGFIX entry was needed.
- 2026-06-26: First independent review after DW lane scheduling found one high-priority RTL protocol bug and several coverage/documentation gaps. The wrapper accepted raw `start_i` in each child schedule, so a held or repeated start could restart CIR/D-RIR independently while simple was still running. Test coverage also checked only final outputs and aggregate counters, not per-cycle lane order. Fixed by adding wrapper-level start-edge gating, holding wrapper `busy_o` until aggregate `done_o`, adding simple/CIR/D-RIR per-cycle trace outputs and Python trace files, stress-testing held `start`, adding `./sim/run_sim.sh all`, and updating documentation.
- 2026-06-26: Recorded the DW reuse start-handshake issue as BUGFIX #006. Verified the review fixes with `./sim/run_sim.sh dw_reuse`, `./sim/run_sim.sh all`, `iverilog -g2012 -Wall -tnull -I rtl rtl/*.v`, and `git diff --check`; all pass.
- 2026-06-26: Added `eg2c_adapt_engine`, `tb_adapt`, and `./sim/run_sim.sh adapt` for the Fig. 6 threshold adaptation flow. The generated target covers signed 8-bit score windows, 8 sensitive-range intervals, ignored out-of-range samples, lower-index argmin tie-break, midpoint threshold update, live-counter reset, and a restarted second window.
- 2026-06-26: Verified threshold adaptation integration: `./sim/run_sim.sh adapt` passes with two generated windows, and `./sim/run_sim.sh all` passes all 13 implemented targets through `adapt`. `iverilog -g2012 -Wall -tnull -I rtl rtl/*.v` and `git diff --check` also pass.
